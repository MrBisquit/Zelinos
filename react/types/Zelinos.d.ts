import { CurrentTab, Settings } from "./types";
export declare const Zelinos: {
    Initialise: (settings: Settings) => void;
    currentTab: CurrentTab;
    settings: Settings;
    IsActiveTab: () => boolean;
};
